/**
 * Tema MindEase — ponto único de entrada.
 * Cores e tokens vêm de palette.ts (fonte da verdade).
 */

export { palette, getCssVars } from './palette'
export type { Palette } from './palette'
export { buildMuiTheme } from './muiTheme'
